# binary add (111 + 001 = 1000)

# LRU cache design
- What to cache?
- Need to validate?
- Only use memory

## LRU cache key point
- Need to get at O(1)
- Need to set at O(1)
- Need to remove oldest item at O(1)

# longest tree in binary tree